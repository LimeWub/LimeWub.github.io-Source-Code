<a name="text"></a>
<section class="section text {{modifiers}}">
    <div class="container">
        {{#if title}}
        <h1 class="h2">{{ title }}</h1>
        {{/if}}
        {{{ text }}}
    </div>
</section>